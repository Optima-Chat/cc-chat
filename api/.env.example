NODE_ENV=development
PORT=3000

# 数据库配置（方式1：使用 DATABASE_URL，Railway/Heroku 会自动提供）
# DATABASE_URL=postgresql://user:password@host:5432/database

# 数据库配置（方式2：单独配置，本地开发使用）
DB_HOST=localhost
DB_PORT=5432
DB_NAME=cc_chat
DB_USER=postgres
DB_PASSWORD=postgres

# Redis 配置
REDIS_HOST=localhost
REDIS_PORT=6379
# REDIS_URL=redis://host:6379
